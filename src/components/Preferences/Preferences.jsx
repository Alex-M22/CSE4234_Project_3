import { useState } from 'react'

function Preferences() {
  const [count, setCount] = useState(0)

    {/* Moved preferences button to Navigation. This is just for the actual card/menu */}

}

export default Preferences
